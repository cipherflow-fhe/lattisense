# LattiSense FPGA Backend

The FPGA backend is no longer supported in this repository.

The previous FPGA runtime and linker submodules have been removed. This directory is retained only as a placeholder to document the deprecation.
